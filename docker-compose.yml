version: '3.8'

services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile.web
    network_mode: "bridge"
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=172.17.0.2
      - DB_PORT=8080
      - DB_NAME=postgres
      - DB_USER=postgres
      - DB_PASS=postgres
